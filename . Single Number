class Solution {
    public int singleNumber(int[] nums) {
        int k=0;
        for (int i=0; i<nums.length; i++){
            k=0;
            for (int j=0;j<nums.length;j++)
            {
                if (nums[i]==nums[j])
                k++;
                }

            
            if (k<2){
                return nums[i];
            
            }

    }

          return 0;
    }
}
